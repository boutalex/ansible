all:
  hosts:
    test:
      ansible_host: 192.168.56.10   # IP de la VM de test
      ansible_user: admin            # utilisateur admin (déjà sudo NOPASSWD)
      ansible_ssh_private_key_file: /var/lib/jenkins/.ssh/id_rsa_ansible
